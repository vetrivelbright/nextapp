import Head from 'next/head'

const about = () => {
    return ( <
        div > < head > < title > Contact Page <
        /title></head > this is contact us page< /div>
    );
}

export default about;